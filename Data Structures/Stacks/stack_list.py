# We use Python Lists as the primary source to build a stack in python.
# The List methods such as append and pop are used to build a stack

stack = []

#Push
stack.append(10)
stack.append(20)
stack.append(30)

#pop
stack.pop()
print(stack)